export default interface DataJob {
  employerRut: string;
  employerName: string;
  contractType: string;
  subscriptionDate: string;
  startDate: string;
  endDate: string;
  activeEmployer: boolean;
}
